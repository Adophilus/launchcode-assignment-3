findByColumnAndValue("Employer", "all") -> findAll();
findByColumnAndValue("all", "val") -> findByValue("val");
findByColumnAndValue("Employer", "Enterprise") -> ArrayList<Job>();

System.out.println(SomeObject.toString())

class="container" some sort of CSS framework is in use
BootStrap

+--------------------------+
|<td>...</td>
|
|
+--------------------------+




? is known as the ternary opetator

Scanner in = new Scanner(System.in);
String userInput = in.nextLine();
String name = (userInput.contains("name:")) ? userInput.split(":")[1] : "undefined!";

		  0, 	1
String[]{name, yourname}

(stdin) name:uchenna:frfr:frfrfr:frfrf:frfrf

stdin.split(":")

String[]{ name, uchenna, frfd, ...}


What is an API?
An API stands for "Application Programming Interface". It functions as a layer
of abstraction between multiple interfaces. 

When dealing with APIs
we usually refer to urls as 'routes'


/list -> Model("list")
/list/jobs -> Model("list-jobs")

what url encoding?
url encoding is the process of converting charaters into a url query format so that
it can be processed by a web server

%20 - space character
page 1.html -> page%201.html

HashMap<String, String> data = new HashMap<String, String>(){
	"column" = "positionType",
	"value" = "Web - Full Stack"
}

data.get("column")


git

A -> proj -> uploads proj
B -> proj -> uploads proj

proj(A) + proj(B)